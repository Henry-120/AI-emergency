# Product

<!-- impeccable:product-schema 1 -->

## Platform

ios

## Register

product

## Stack

既有程式碼已決定：React 19 + TypeScript + Vite（前端）、Tailwind 語意化 token（樣式）、Capacitor 8 打包為 iOS App、FastAPI + Cloud Firestore（後端，Docker / Cloud Run）。

**2026-08-24 設計方向決策**：使用者最初選擇「改成 iOS 原生語言（HIG）」，但在說明真正的訴求是「原本的設計太有 AI 感」之後，改為**實作既有的 DESIGN.md 自訂視覺系統**，並補上 iOS 的結構慣例（Dynamic Type、44pt 觸控目標、安全區）。

技術堆疊不變：App 仍是 Capacitor 包裝的 React 網頁。視覺識別走 DESIGN.md 的「儀器級災防」系統，不是 Apple HIG 的原生外觀。同日已完成實作：19 個元件檔中 321 處寫死的 Tailwind 色彩全數改為語意 token，並移除金色漸層、玻璃擬態、金色光暈、側邊粗色條等被 DESIGN.md 列為禁止的樣式。若日後要改寫為 SwiftUI 原生，需另行確認，本檔未記錄該決定。

## Users

一般民眾，在地震、火災、淹水等突發災害的**當下或剛發生後**使用。典型情境：剛感受到劇烈搖晃或聞到煙味，腎上腺素飆高、雙手顫抖、可能只有一隻手能操作手機，環境可能是昏暗的樓梯間、停電的室內、或強光的戶外街道。他們不是消防或救難專業人員，沒有時間學習介面。

要完成的工作（job to be done）：**在最短時間內知道「現在這一秒該做的下一個動作」**，並在沒有網路時仍能取得求生指引、找到最近的避難所、與附近的人聯繫。

## Product Purpose

GuardiaAI 是一個以 AI 對話為核心的災害應變求生助手。使用者用文字或語音描述現場狀況（或上傳照片），系統回傳分級的即時行動指令、狀況分析與生存評估；同時整合中央氣象署（CWA）即時地震警報、離線地圖與避難所導航、藍牙（BLE）近距離訊息，確保在斷網、停電的災害環境下仍可運作。

成功的定義：在使用者最慌張的一兩秒內，介面就能讓他**讀懂並執行最高優先的那一個動作**，而不是讓他先理解介面。

## Positioning

多數災防 App 是「單向廣播」：把官方警報推給你，然後就沒了。GuardiaAI 的差異機制是**基礎設施全倒之後仍然成立**——三件事同時為真，鄰近產品無法誠實照抄：

1. **斷網仍有 AI**：裝置端內建 llama.cpp 模型（`src/services/offlineService.ts`），沒有網路時由手機本機產生行動指令，不是退化成一份靜態 PDF 求生手冊。
2. **無基地台仍能傳訊**：BLE 多跳 SOS 中繼（`src/services/sos/`，含 TTL 遞減與省電式 relay selection、端對端加密），求救訊息可經由附近其他裝置接力傳出。
3. **官方資料為底**：警報與避難所不是自製資料，而是 CWA 與內政部消防署的真實資料源。

一句話定位：**當網路、電力、基地台都失效時，仍然會回答你「下一步做什麼」並把你的求救送出去的助手。**

## Operating Context

- **使用當下**：災害發生的頭幾分鐘到幾小時。手機可能只剩單手可用、電量低、螢幕在強光或全黑環境下。
- **網路狀態是光譜，不是開關**：全連線 → 微弱／間歇 → 完全斷網。App 需在三種狀態間自動切換（線上走 Gemini + 後端，離線走裝置端模型 + 本機 SQLite/localStorage 快取，恢復連線後由 `offlineQueueService` 補送）。
- **電力是設計條件**：低電量（≤20%、未充電）會強制切換深色主題以省電。
- **裝置間協作**：附近其他使用者的手機是網路的一部分（BLE 掃描、鄰近人員清單、聊天、SOS 中繼）。
- **主要地區**：台灣，繁體中文（zh-TW），資料與座標以台灣為範圍。

## Capabilities and Constraints

**已實作的能力**
- AI 災況對話：文字／語音／照片輸入，回傳分級行動指令（CRITICAL / HIGH / MEDIUM）、狀況分析與生存評估。
- CWA 即時地震警報與跑馬燈通知。
- 離線地圖與避難所導航（MapLibre + PMTiles 向量圖磚、離線圖磚下載）。
- 藍牙近距離：鄰近裝置清單、聊天、加密 SOS 廣播與多跳中繼。
- 醫療卡（過敏、慢性病、緊急聯絡人），供救援者讀取。
- AR 房間風險掃描（拍照分析室內危險物）。
- 健康資料整合（心率等）、TTS 語音播報、語音輸入。
- 帳號註冊登入、Firestore 同步、推播通知。

**限制與待確認事項**
- 推播通知：程式碼已完成，**卡在需要付費 Apple Developer 帳號才能產生 APNs 金鑰**（未解）。
- 需要的外部金鑰：`GEMINI_API_KEY`、`CWA_API_KEY`、Firebase service account。缺任一項時對應功能降級。
- Firebase 專案為 `guardiaai-e92be`。
- 免責聲明流程：首次啟動有兩步驟同意畫面，屬法律必要流程，不可為了視覺簡潔而移除。
- App ID `com.guoshuohong.guardiaai`，顯示名稱目前為 `Guardian`（與產品名 GuardiaAI 不一致，尚未決定要統一成哪一個）。

**術語**
「行動指令 / SurvivalStep」帶有 priority（CRITICAL / HIGH / MEDIUM）與排序編號；「避難所（shelter）」指官方避難收容處所；「SOS 中繼（relay）」指經他人裝置接力轉傳的求救訊息。

## Brand Commitments

- 產品名 **GuardiaAI**。
- 主要語言繁體中文（zh-TW），字型需完整支援 CJK。
- 語氣與人格已確認並綁定，見下方 `## Brand Personality`；`## Anti-references` 為明確禁止項。

## Brand Personality

冷靜、權威、可信賴（Calm, Authoritative, Trustworthy）。語氣像一位受過訓練、在你耳邊沉穩下達指令的緊急救護指揮，而不是會閃爍尖叫的警報器。視覺像官方災防告警系統與飛安須知卡：資訊分明、毫不含糊、降低恐慌。情緒目標是**穩住使用者**，用清晰度而非刺激度傳達急迫性。

## Anti-references

- **原本的深色＋金色奢華風**：金色光暈（gold-glow）、漸層金色泡泡，把求生工具做得像高級會員 App，不合情境，要完全捨棄。
- **「警報疲勞」式滿屏紅 / 高飽和橘紅 + 脈動閃爍**：通篇高張力反而稀釋了真正 CRITICAL 的訊號，也讓慌張者更焦慮。
- **消費級花俏感**：玻璃擬態裝飾、彩底配灰字、卡片單側粗色條（border-l-4）、彩色光暈——皆為 AI 爛設計樣式，明確避免。
- **冷淡到沒有權威感的「療癒系極簡」**：避開「不紅就軟綿綿」的二階反射；要的是儀器級的清楚與分量，不是 wellness App 的輕飄。
- 不要 SaaS 儀表板的 hero 大數字模板、不要每段都加小字大寫 eyebrow 標籤。

## Evidence on Hand

**真實、不可竄改（使用者於 2026-08-24 逐項確認）**
- **官方避難所點位 5,973 筆** — `data/shelters/taiwan_shelters.json`，來源標示「內政部消防署 避難收容處所點位檔」。
- **CWA 中央氣象署即時地震警報** — 串接真實 API（`src/services/cwaService.ts`、`backend/services/cwa_service.py`），需 `CWA_API_KEY`。
- **AI 回覆由 Google Gemini 即時產生** — `src/services/geminiService.ts`，非預先寫死的腳本；離線時改由裝置端 llama.cpp 模型產生。
- `data/shelters/seed_shelters.json` 是**內建備援示範資料**（桃園中壢周邊），其檔內來源欄位已自述「正式上線前請以政府開放資料快取取代」——展示時不可把它當成全國資料呈現。

**明確不存在，任何設計都不得編造**
- 沒有真實使用者、下載數、活躍人數。
- 沒有使用者見證、評價、成功救援案例。
- 沒有客戶、合作單位、政府背書、獎項或媒體報導。
- 沒有效能基準數據、準確率數字、SLA 或定價。

未來若要在任何畫面放「社會證明」類內容，必須先取得真實素材，不可以先放假的佔位文案。

## Product Principles

1. **斷網是常態，不是錯誤狀態**：離線路徑與線上路徑同等重要，離線能力要被當成賣點呈現，而非降級提示。
2. **每個畫面只有一個主角**：此刻該做的那一件事必須無可爭辯地最顯眼，其餘退居輔助。
3. **展示與實用不可互相犧牲**（使用者於 2026-08-24 決定「兩者都要」）：介面要能在評審面前說清楚價值，但每個設計決策仍以「真的能在災害現場用」為判準；不做只為展示的假功能或假資料。
4. **只說得出來源的話**：警報、避難所、AI 回覆都可追溯到真實資料源；產品敘述不超過 `## Evidence on Hand` 允許的範圍。
5. **在最壞的操作條件下仍然成立**：單手、顫抖、強光、低電量、昏暗——這是設計的基準條件，不是邊緣案例。

## Design Principles

1. **下一個動作優先（Next-action first）**：每個畫面都要有一個無可爭辯的視覺主角——此刻該做的那一件事。其餘資訊退居輔助。
2. **用清晰傳達急迫，而非用噪音（Urgency through clarity, not noise）**：層次、對比、留白與字級扛起急迫感；紅色只保留給最高危的 CRITICAL，少用才有力。
3. **斷網也要完整（Whole even when offline）**：離線是常態而非例外，離線狀態與離線功能要一等公民地呈現，不能像降級的錯誤畫面。
4. **單手、顫抖、強光下都讀得到（Readable one-handed, shaking, in glare）**：大觸控目標、拇指可及的主要操作、高對比、特大關鍵字。
5. **儀器而非玩具（An instrument, not a toy）**：一致、可預期、零裝飾性動效；介面消失在任務之後。

## Accessibility & Inclusion

- 目標 WCAG 2.1 AA，關鍵指令文字力求 AAA（≥7:1）對比；body 文字一律 ≥4.5:1，不用淺灰字「裝氣質」。
- **減少動態為第一優先**：所有動效都要有 `prefers-reduced-motion: reduce` 的替代（淡入或直接顯示）；內容預設可見，不靠動畫才出現。
- **語音為第一優先**：關鍵行動指令可語音播報（已有 TTS 與語音輸入），語音狀態有 `aria-live` 提示。
- 優先級不只靠顏色：CRITICAL/HIGH/MEDIUM 同時用圖示、文字標籤與形狀區分，紅綠色盲也能辨識。
- 自動跟隨系統明暗（含低電量/夜間情境），兩套主題都需通過對比檢查。
- 大觸控目標（主要操作 ≥48px），主操作集中於畫面下半拇指熱區。
- 繁體中文（zh-TW）為主要語言，字型需完整支援 CJK。
- 平台改為 iOS 後，另需符合 Apple 無障礙慣例：VoiceOver 標籤、Dynamic Type（字級可放大）、最小 44×44pt 觸控目標。以上兩套標準取較嚴者。
